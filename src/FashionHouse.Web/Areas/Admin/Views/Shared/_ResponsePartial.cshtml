@using FashionHouse.Infrastructure.Extensions
@using FashionHouse.Web.Areas.Admin.Models

@if (TempData.Peek<ResponseModel>("ResponseMessage") != null)
{
    var response = TempData.Get<ResponseModel>("ResponseMessage");
    <div class="alert alert-@(response.Type.ToString().ToLower()) alert-dismissible" role="alert">
        @response.Message
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}
else
{
    <div></div>
}